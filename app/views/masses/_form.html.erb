<%= form_for(@mass) do |f| %>
  <% if @mass.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mass.errors.count, "error") %> prohibited this mass from being saved:</h2>

      <ul>
      <% @mass.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :day %><br />
    <%= f.text_field :day %>
  </div>
  <div class="field">
    <%= f.label :time %><br />
    <%= f.text_field :time %>
  </div>
  <div class="field">
    <%= f.label :type %><br />
    <%= f.text_field :type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
